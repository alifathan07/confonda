<!-- Topbar -->
<nav class="navbar navbar-expand navbar-light bg-white shadow-sm topbar mb-4 " style="margin-left: -200px;">
    <!-- Sidebar Toggle (Mobile) -->
    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
        <i class="fa fa-bars"></i>
    </button>

    <ul class="navbar-nav ml-auto align-items-center">

        <!-- Notifications -->
        <li class="dropdown mx-2">
            <a href="#" class="dropdown-toggle position-relative text-dark" id="alertsDropdown">
                <i class="fas fa-bell fa-fw" style="font-size:1.2rem;"></i>
                <span class="badge badge-danger" style="position:absolute; top:-5px; right:-5px; font-size:0.65rem;">3</span>
            </a>
            <div class="dropdown-menu shadow rounded-lg" aria-labelledby="alertsDropdown">
                <h6 class="dropdown-header px-3 py-2">Notifications</h6>
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="#">
                    <div class="icon-circle bg-primary mr-3 d-flex justify-content-center align-items-center">
                        <i class="fas fa-file-alt text-white"></i>
                    </div>
                    <div>
                        <div class="small text-gray-500">2 mins ago</div>
                        <span>New report available</span>
                    </div>
                </a>
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="#">
                    <div class="icon-circle bg-success mr-3 d-flex justify-content-center align-items-center">
                        <i class="fas fa-check text-white"></i>
                    </div>
                    <div>
                        <div class="small text-gray-500">1 hr ago</div>
                        <span>Chèque validé</span>
                    </div>
                </a>
                <a class="dropdown-item text-center small text-gray-500 px-3 py-2" href="#">See All Notifications</a>
            </div>
        </li>

        <!-- Messages -->
        <li class="dropdown mx-2">
            <a href="#" class="dropdown-toggle position-relative text-dark" id="messagesDropdown">
                <i class="fas fa-envelope fa-fw" style="font-size:1.2rem;"></i>
                <span class="badge badge-success" style="position:absolute; top:-5px; right:-5px; font-size:0.65rem;">5</span>
            </a>
            <div class="dropdown-menu shadow rounded-lg" aria-labelledby="messagesDropdown">
                <h6 class="dropdown-header px-3 py-2">Messages</h6>
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="#">
                    <div class="dropdown-list-image mr-3">
                        <img class="rounded-circle" src="img/undraw_profile_1.svg" alt="..." style="width:35px;height:35px;">
                        <div class="status-indicator bg-success"></div>
                    </div>
                    <div>
                        <div class="text-truncate">Hi there! I need your help with the budget report.</div>
                        <div class="small text-gray-500">Emily · 58m</div>
                    </div>
                </a>
                <a class="dropdown-item text-center small text-gray-500 px-3 py-2" href="#">Read More Messages</a>
            </div>
        </li>

        <!-- User Profile -->
        <li class="dropdown mx-2">
            <% if(user){ %>
            <a href="#" class="dropdown-toggle d-flex align-items-center text-dark" id="userDropdown">
                <span class="mr-2 d-none d-lg-inline font-weight-medium text-gray-700"><%= user.name %></span>
                <img class="rounded-circle" src="<%= user.avatar || 'img/undraw_profile.svg' %>" style="width:40px;height:40px;">
            </a>
            <div class="dropdown-menu shadow rounded-lg" aria-labelledby="userDropdown">
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="#">
                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i> Profile
                </a>
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="/settings">
                    <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i> Settings
                </a>
                <a class="dropdown-item d-flex align-items-center px-3 py-2" href="#">
                    <i class="fas fa-list fa-sm fa-fw mr-2 text-gray-400"></i> Activity Log
                </a>
                <div class="dropdown-divider"></div>
                <form action="/logout" method="POST" class="m-0 p-0">
                    <button type="submit" class="dropdown-item d-flex align-items-center px-3 py-2">
                        <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i> Logout
                    </button>
                </form>
            </div>
            <% } %>
        </li>

    </ul>
</nav>

<style>
/* Dropdown hidden by default */
.dropdown-menu {
    display: none;
    position: absolute;
    top: 100%;
    right: 0;
    z-index: 1000;
    min-width: 220px;
    background-color: #fff;
}

/* Show dropdown */
.dropdown.show > .dropdown-menu {
    display: block;
}

/* Circle icons */
.icon-circle {
    width: 35px;
    height: 35px;
    border-radius: 50%;
}

/* Status indicator */
.status-indicator {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    border: 1px solid #fff;
    position: absolute;
    bottom: 0;
    right: 0;
}
</style>

<script>
document.querySelectorAll('.dropdown-toggle').forEach(toggle => {
    toggle.addEventListener('click', function(e){
        e.preventDefault();
        const parent = this.parentElement;
        parent.classList.toggle('show');

        // Close others
        document.querySelectorAll('.dropdown').forEach(drop => {
            if(drop !== parent) drop.classList.remove('show');
        });
    });
});

// Close dropdown when clicking outside
document.addEventListener('click', function(e){
    if(!e.target.closest('.dropdown')){
        document.querySelectorAll('.dropdown').forEach(drop => drop.classList.remove('show'));
    }
});
</script>
