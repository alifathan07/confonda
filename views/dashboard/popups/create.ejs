<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <title>Créer un Popup - Developer Dashboard</title>
  <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
  <link href="/vendor/fontawesome-free/css/all.min.css" rel="stylesheet">
  <link href="/css/sb-admin-2.min.css" rel="stylesheet">

  <style>
    .type-option {
      cursor: pointer;
      padding: 1rem;
      border: 2px solid #e3e6f0;
      border-radius: 0.5rem;
      text-align: center;
      transition: all 0.2s;
    }
    .type-option:hover,
    .type-option.selected {
      border-color: #4e73df;
      background: #f8f9fc;
    }
    .type-option.selected {
      background: #e8eaff;
    }
    .type-info.selected { border-color: #17a2b8; background: #e8f4f8; }
    .type-warning.selected { border-color: #ffc107; background: #fff8e1; }
    .type-success.selected { border-color: #28a745; background: #e8f5e9; }
    .type-error.selected { border-color: #dc3545; background: #ffe5e5; }
  </style>
</head>

<body id="page-top">
  <div id="wrapper">
    <%- include('../../layouts/sidebar') %>
    <div id="content-wrapper" class="d-flex flex-column">
      <div id="content">
        <%- include('../../layouts/topbar') %>
        <div class="container-fluid">

          <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="h3 mb-0 text-gray-800">
              <i class="fas fa-bullhorn mr-2"></i>Créer un Popup
            </h1>
            <a href="/popups" class="btn btn-secondary">
              <i class="fas fa-arrow-left mr-2"></i>Retour
            </a>
          </div>

          <div class="card shadow">
            <div class="card-header py-3">
              <h6 class="m-0 font-weight-bold text-primary">Configuration du Popup</h6>
            </div>
            <div class="card-body">
              <form id="popupForm">
                <!-- Title -->
                <div class="form-group">
                  <label for="title" class="font-weight-bold">Titre <span class="text-danger">*</span></label>
                  <input type="text" class="form-control" id="title" name="title" 
                    placeholder="Titre du popup..." required>
                </div>

                <!-- Message -->
                <div class="form-group">
                  <label for="message" class="font-weight-bold">Message <span class="text-danger">*</span></label>
                  <textarea class="form-control" id="message" name="message" rows="4" 
                    placeholder="Message à afficher aux utilisateurs..." required></textarea>
                </div>

                <!-- Type -->
                <div class="form-group">
                  <label class="font-weight-bold d-block">Type de popup <span class="text-danger">*</span></label>
                  <div class="row">
                    <div class="col-md-3 mb-3">
                      <div class="type-option type-info selected" data-type="info" onclick="selectType('info')">
                        <i class="fas fa-info-circle fa-2x text-info mb-2"></i>
                        <div class="font-weight-bold">Information</div>
                      </div>
                    </div>
                    <div class="col-md-3 mb-3">
                      <div class="type-option type-warning" data-type="warning" onclick="selectType('warning')">
                        <i class="fas fa-exclamation-triangle fa-2x text-warning mb-2"></i>
                        <div class="font-weight-bold">Attention</div>
                      </div>
                    </div>
                    <div class="col-md-3 mb-3">
                      <div class="type-option type-success" data-type="success" onclick="selectType('success')">
                        <i class="fas fa-check-circle fa-2x text-success mb-2"></i>
                        <div class="font-weight-bold">Succès</div>
                      </div>
                    </div>
                    <div class="col-md-3 mb-3">
                      <div class="type-option type-error" data-type="error" onclick="selectType('error')">
                        <i class="fas fa-times-circle fa-2x text-danger mb-2"></i>
                        <div class="font-weight-bold">Erreur</div>
                      </div>
                    </div>
                  </div>
                  <input type="hidden" name="type" id="type" value="info">
                </div>

                <hr>

                <div class="d-flex justify-content-between">
                  <a href="/popups" class="btn btn-secondary">Annuler</a>
                  <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save mr-2"></i>Créer le popup
                  </button>
                </div>
              </form>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>

  <script src="/vendor/jquery/jquery.min.js"></script>
  <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
  <script src="/vendor/jquery-easing/jquery.easing.min.js"></script>
  <script src="/js/sb-admin-2.min.js"></script>

  <script>
    function selectType(type) {
      $('.type-option').removeClass('selected');
      $(`.type-option[data-type="${type}"]`).addClass('selected');
      $('#type').val(type);
    }

    $('#popupForm').submit(function(e) {
      e.preventDefault();

      const formData = {
        title: $('#title').val(),
        message: $('#message').val(),
        type: $('#type').val()
      };

      $.ajax({
        url: '/api/popups',
        method: 'POST',
        contentType: 'application/json',
        data: JSON.stringify(formData),
        success: function(data) {
          if (data.success) {
            window.location.href = '/popups';
          } else {
            alert(data.error || 'Erreur lors de la création');
          }
        },
        error: function() {
          alert('Erreur lors de la création du popup');
        }
      });
    });
  </script>
</body>

</html>
